<%= simple_form_for @event, remote: true do |f| %>
  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :date_range, input_html: { class: "form-control input-sm date-range-picker" } %></br>
    <%= f.input_field :start, input_html: { class: "form-control input-sm date-range-picker" } %></br>
    <%= f.input_field :end, input_html: { class: "form-control input-sm date-range-picker" } %></br>
    <%= f.input :color, as: :select, collection: [['Black','rgb(255, 179, 197)'], ['Green','green'], ['Red','red']] %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
    <%= link_to 'Delete', 
                event, 
                method: :delete, 
                class: 'btn btn-danger', 
                data: { confirm: 'Are you sure?' }, 
                remote: true unless @event.new_record? %>
  </div>
<% end %>